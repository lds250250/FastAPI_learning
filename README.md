# FastAPI 学习路线与进度追踪

## 第 0 周：环境准备与工具链打通（已完成）（已完成）
- **Day 1**：安装 Python 3.10+、Git、VS Code 及必备插件，配置 GitHub Copilot。
- **Day 2**：配置 GitHub 账号，生成并配置 SSH 密钥，理解公钥与私钥的安全机制。
- **Day 3**：安装 `uv` 包管理器，解决环境变量问题，使用 `uv sync` 同步环境并成功跑通 FastAPI 的 Hello World。

## 第一阶段：FastAPI 核心基础与 Pydantic 数据校验（第 1 周）（已完成）
- **Day 1**：路径参数与查询参数（Path & Query Params）。
- **Day 2**：Pydantic 请求体与数据模型（BaseModel）。
- **Day 3**：Pydantic 进阶校验（Field & Validators，包括 `...` 和 `| None` 的用法）。
- **Day 4**：响应模型（Response Model）与出口把关。
- **Day 5**：状态码（Status Codes）与 RESTful 规范。
- **Day 6**：接口进阶与文档规范（Tags、Summary、HTTPException 主动抛错机制）。
- **Day 7**：第一周实战任务（开发一个包含增删改查的极简工具接口项目，巩固本周所有知识点）。

## 第二阶段：核心特性与架构拆分（第 2 周）（已完成）
- **Day 1**：Pydantic V2 核心配置与模型规范（`model_config`）。
- **Day 2**：高级字段校验（`field_validator` 与 `model_validator`）。
- **Day 3**：响应模型进阶与序列化控制（`exclude` 与 `alias`）。
- **Day 4**：`Annotated` 注解与自定义类型、`TypeAdapter`。
- **Day 5**：请求模型与响应模型分离（Create / Read / Update 架构）。
- **Day 6**：第二周综合实战（极简图书管理系统 CRUD）。

## 第二阶段：核心特性与架构拆分（第 3 周）（已完成）
- **Day 1**：用户注册模型与嵌套校验（`EmailStr`、嵌套 Profile 模型）。
- **Day 2**：密码处理与敏感字段过滤（模拟哈希、`exclude=True`）。
- **Day 3**：用户登录逻辑与错误处理（`fake_users_db`、`HTTPException`）。
- **Day 4**：综合实战（上）—— 完善用户 CRUD 与密码安全更新。
- **Day 5**：综合实战（下）—— 代码重构、`Annotated` 优化与模板化。

## 第三阶段：架构拆分与依赖注入（第 4 周）（已完成）
- **Day 1**：环境变量管理与配置解耦（`pydantic-settings`、`.env` 文件与 `Settings` 单例）。
- **Day 2**：分层架构设计与目录重构（四层职责划分、依赖方向与 `src-layout` 目录树）。
- **Day 3**：数据层封装（把 `fake_users_db` 抽成 `Repository`，数据层不碰 HTTP）。
- **Day 4**：业务层封装（把密码哈希与业务规则抽成 `Service`）。
- **Day 5**：路由层瘦身与 `APIRouter` 模块化组装（`prefix`、`tags` 与多模块注册）。
- **Day 6**：各层串联与依赖落地（`deps.py` 提供 `repo` / `service` / `current_user` 依赖）。
- **Day 7**：第四周综合实战（上）—— 按分层架构新增一个业务模块，独立完成 CRUD。
- **Day 8**：第四周综合实战（下）—— 全接口兼容性验证、补充 `.env.example`、更新 README 与架构笔记。

## 第三阶段：架构拆分与依赖注入（第 5 周）（已完成）
- **Day 1**：类作为依赖（`__init__` 声明参数、`__call__` 返回，把一组查询参数封装成对象）。
- **Day 2**：依赖的解析机制（全局 / 路由级 / 单接口级作用域，与请求级缓存 `use_cache`）。
- **Day 3**：带清理的依赖（`yield` 依赖与资源管理，为数据库会话铺路）。
- **Day 4**：权限拦截依赖（读取 `Header`、401 与 403 语义、角色校验依赖）。
- **Day 5**：限流与请求日志依赖（内存计数器限流、429、请求耗时统计）。
- **Day 6**：第五周综合实战 —— 给用户与图书模块接入分页、权限、限流依赖，并更新 README。

## 第三阶段：架构拆分与依赖注入（第 6 周）（已完成）
- **Day 1**：中间件基础（CORS 跨域、自定义中间件）。
- **Day 2**：请求日志中间件与「依赖 vs 中间件」的分工边界。
- **Day 3**：全局异常处理器（统一错误响应格式）。
- **Day 4**：自定义业务异常体系（Service 抛业务异常，由处理器统一翻译成 HTTP）。
- **Day 5**：第六周综合实战 —— 给现有模块接入中间件与统一异常处理，并更新 README。

## 第四阶段：测试、认证与异步数据库（第 7 周）（已完成）
- **Day 1**：`pytest` 入门与测试目录组织（命名规范、断言、运行方式）。
- **Day 2**：`fixture` —— 测试世界的「依赖注入」。
- **Day 3**：`TestClient` 接口测试（状态码与响应体断言）。
- **Day 4**：`dependency_overrides`（测试时把真实依赖替换成假实现）。
- **Day 5**：分层测试（脱离 HTTP 单测 Service 与 Repository，理解「测行为，不测实现」）。
- **Day 6**：第七周综合实战 —— 给用户与图书模块补齐测试，并更新 README。

## 第四阶段：测试、认证与异步数据库（第 8 周）（已完成）
- **Day 1**：密码哈希（`bcrypt` 加盐哈希与校验，替换第 3 周的假哈希）。
- **Day 2**：OAuth2 密码流与 `/token` 表单接口（`OAuth2PasswordRequestForm`）。
- **Day 3**：JWT 结构与令牌生成（`SECRET_KEY`、`ALGORITHM` 终于派上用场）。
- **Day 4**：令牌校验与调用者身份（`OAuth2PasswordBearer`、从 `Authorization` 头解析调用者，新增 `GET /users/me`）。
- **Day 5**：切换认证方案（用户增加 `role` 字段、注册接口开放、删除 API Key 方案并回归测试）。
- **Day 6**：令牌过期与刷新（`exp` 声明与刷新思路）。
- **Day 7**：基于角色的权限控制（令牌携带角色，细化授权规则）。
- **Day 8**：第八周综合实战 —— 完成登录闭环并保护现有接口，更新 README。

## 第四阶段：测试、认证与异步数据库（第 9 周）（已完成）
- **Day 1**：同步、并发、异步各自在解决什么问题。
- **Day 2**：事件循环与 `await` 的本质。
- **Day 3**：假异步 —— 在 `async def` 里写阻塞代码的代价。
- **Day 4**：并发编排（`asyncio.gather` 与任务）。
- **Day 5**：第九周综合实战 —— 对比串行 / 并发 / 假异步三种写法的耗时。

## 第四阶段：测试、认证与异步数据库（第 10 周）（已完成）
- **Day 1**：异步数据库引擎与连接配置（`create_async_engine`、`DATABASE_URL`）。
- **Day 2**：连接与会话管理（`async_sessionmaker` 与第 5 周学的 `yield` 依赖）。
- **Day 3**：SQLAlchemy 2.0 ORM 模型（`DeclarativeBase`、`Mapped`、`mapped_column`）。
- **Day 4**：建表与初始化数据（`create_all` 与种子数据）。
- **Day 5**：第十周综合实战 —— 打通「引擎 + 会话 + 模型」，能连上真实数据库。

## 第四阶段：测试、认证与异步数据库（第 11 周）（已完成）
- **Day 1**：仓储层改造（上）—— 用户模块：`UserRepository` 从内存字典换成异步 SQL 查询，服务层异步化，依赖注入接入数据库会话。
- **Day 2**：仓储层改造（下）—— 图书模块照同一模式落地，识别"能照抄"与"不能照抄"的边界。
- **Day 3**：测试隔离 —— 用「每个测试一套一次性数据库」替换掉清内存字典的做法，让 53 个 API 测试和 4 个数据库测试重新跑起来（这些测试本身一行都不用改）。
- **Day 4**：分层测试修复 —— 25 个 Repository / Service 单测的异步化，以及数据库约束带来的连锁影响。
- **Day 5**：借阅记录表与外键 —— 新增 `borrow_records` 模型与两条外键，一对多建模的取舍（为什么不往 `books` 上加一列），以及 SQLite「外键约束默认关闭」这个坑（需要在每条连接上 `PRAGMA foreign_keys=ON`）。
- **Day 6**：借阅闭环 —— 改写 `borrow`（减库存 + 写借阅记录，把「检查 + 扣减」压进一条 SQL 解决超卖）、新增归还接口，以及配套测试。
- **Day 7**：查询下推与关联加载 —— 分页、排序、筛选从 Python 层下推给数据库；`relationship` 的声明，以及异步下必须预加载（`selectinload`）这个坑。
- **Day 8**：第十一周综合实战 —— 全模块接入数据库，并验证第 7 周写的测试全部存活。

> **计划调整说明（第 11 周）**：原定 6 天，顺序为「表关系 → Alembic → 迁移升降级 → 分页 → 实战」。
> 实际执行时做了五处调整：
> 1. **「仓储层改造」拆成 Day 1 / Day 2 两天**（用户模块、图书模块），因为改动横跨 5 个文件，合在一起不好验证。
> 2. **把「测试隔离」提前到 Day 3。** 原因是 Day 1 之后 82 个测试全部失效（它们靠清内存字典来隔离数据），在没有任何回归网的状态下新增表和外键，一旦出错将无法判断问题来自新代码还是既有的断裂。Day 3 之后发现破坏面比预想的大 —— 除了 `conftest.py`，还有 25 个直接构造 Repository / Service 的单测也断了，于是拆出 Day 4 专门处理。
> 3. **原 Day 4「迁移的升级、回滚与版本管理」并入 Alembic 当天**，腾出的时间用于新增「借阅闭环」，用来补上项目里 `borrow` 只减库存、不记录借阅人的真实缺口。
> 4. **把原本一天的「表关系与借阅闭环」拆成 Day 5 / Day 6**，Day 5 只立数据模型（模型 + 外键 + SQLite 外键开关），Day 6 才做接口和并发。原因是合在一起时，一节课里同时要理解"数据怎么关联"和"并发怎么正确"两件互不相关的事。
> 5. **Alembic 从第 11 周挪到第 15 周（部署运维）。** 迁移这件事的本质是「让已经在运行、已经有数据的系统平滑地改表结构」，它属于部署范畴。第 11 周的库里只有可随时重建的种子数据，`create_all` 完全够用，此时学 Alembic 会变成"为了学而学"；而第 15 周要上 Docker + Postgres，那时才会真的遇到「生产库不能删库重建」的场景。

## 第五阶段：缓存、实时通信与可观测性（第 12 周）（已完成）
- **Day 0**：环境准备 —— 准备一个可用的 Redis 服务。**三种方案任选其一，代码完全一样，只需要改 `REDIS_URL` 一行**：
  - **① 云 Redis（Upstash，实际采用）** —— 不装任何东西，注册后从控制台拿 `rediss://` 连接串填进 `.env`。
  - ② Memurai —— 原生 Windows 服务，装完即用。
  - ③ Docker —— `docker run -d --name my-redis -p 6379:6379 -v redis-data:/data redis:7-alpine`。
  - **不采用** `fakeredis`：它是进程内模拟，演示不了本周 Day 3「多进程 / 多实例共享计数」这个核心点。
  **不讲** Dockerfile、多阶段构建、Compose 编排与镜像瘦身 —— 那些仍按原计划留在第 15 周。
- **Day 1**：Redis 连接与异步客户端（`redis.asyncio`、连接配置进 `.env`）。
- **Day 2**：缓存模式（读缓存、缓存失效、缓存穿透的基本应对）。
- **Day 3**：把内存限流升级为 Redis 限流（多进程 / 多实例共享计数）。
- **Day 4**：限流的原子性与算法取舍 —— 复盘 Day 3 的 ZSET 方案：它的「检查」和「记录」是两条 Redis 命令，中间隔着 `await`，并发下会多放行（内存版因为中间没有 `await` 反而是安全的，是"搬进 Redis"这一步引入了竞态）；改用 `INCR` 实现固定窗口限流（单条 Redis 命令天然原子），并对比固定窗口 / 滑动窗口 / 令牌桶的取舍。
- **Day 5**：Redis 挂掉之后 —— 缓存与限流的降级。缓存是「加速」不是「依赖」，Redis 不可用时应退回数据库而不是让接口 500；限流要在 fail-open（放行）与 fail-closed（全拒）之间做选择；以及「哪些异常才算挂了」（只抓连不上 / 超时，不能吞掉命令写错的那类异常）。
- **Day 6**：第十二周综合实战 —— 限流限谁（只按 IP 会连坐、换 IP 就绕过）、全周验收，并更新 README。

> **计划调整说明（第 12 周）**：Redis 官方不提供 Windows 版本，第 12 周必须先有一个能用的 Redis 服务。
> 原方案是把第 15 周的 Docker 内容整体提前 —— 但那会打乱第 15 周（Nginx、Gunicorn 进程模型、镜像瘦身）的结构。
> 第一次尝试是「加一个 Day 0，用 Docker 跑一个容器」。**实际执行时 Docker 的环境成本过高**（Windows 上需要 WSL2 / Hyper-V / BIOS 虚拟化），于是改用**云 Redis（Upstash 免费版）**，Day 0 的范围也随之从"学 Docker 命令"变成"拿到一个连接串"。
> **这件事本身值得记下来**：三种方案的代码完全相同，因为地址被抽在 `REDIS_URL` 里 —— **配置外置的价值在这里第一次真正兑现。** 将来从 SQLite 换 Postgres 也是同一回事。
> **Day 4 的调整**：原计划是「防刷与滑动窗口（更精确的限流算法）」。但 Day 3 实际落地的 ZSET 滑动窗口日志**本身就是**"更精确的算法"，原 Day 4 失去了对象。真正该处理的，是 Day 3 实现里被发现的一个缺陷：**ZSET 方案的「检查」与「记录」是两条独立的 Redis 命令，中间隔着 `await`，并发下会多放行** —— 而内存版因为中间没有 `await` 反而是安全的，**是"搬进 Redis"这一步引入了竞态**。于是 Day 4 改为处理这个真实缺陷，并顺带完成算法选型（固定窗口 / 滑动窗口 / 令牌桶）。
> **Day 5 的调整**：原计划 Day 5 是「综合实战 —— 给图书模块接入缓存与分布式限流」，但缓存 Day 2 已接入、限流 Day 3/4 已接入，**这个实战日失去了对象**。复盘代码时发现一个更要紧的问题：**Redis 目前是硬依赖** —— 缓存读失败会让每个图书详情请求直接 500，限流失败会让登录直接 500。**缓存本该是"加速"，却变成了"没有它就活不了"**，而 Upstash 免费版有每日命令配额、网络会抖动、容器会重启，这件事一定会发生。于是 Day 5 改为处理这个单点故障（降级），原定的「限流限谁」并入 Day 6 的实战。

## 第五阶段：缓存、实时通信与可观测性（第 13 周）
- **Day 1**：WebSocket 基础与握手 —— 与 HTTP 的根本差异（长连接、双向、没有「一问一答」的配对）、`/ws` 端点、最小 echo；以及 `TestClient.websocket_connect` 怎么测。
- **Day 2**：WebSocket 鉴权 —— **为什么不能照搬第 8 周的 `OAuth2PasswordBearer`**（浏览器的 WebSocket API 不允许自定义请求头，`Authorization` 根本发不出去），改用查询参数或首条消息携带令牌，并在 `accept()` 之前就拒绝非法连接。
- **Day 3**：连接管理器 —— 登记连接、按用户名维护在线名单、断开时清理。**注意它住在进程内存里，和第 12 周 Day 3 之前的 `_hits` 是同一个东西。**
- **Day 4**：消息收发与广播 —— 单播、群发、`WebSocketDisconnect` 的捕获与清理。
- **Day 5**：多实例下的广播 —— 连接字典只在**单个进程内**有效，多 worker 时「连着 A 进程的人」收不到「从 B 进程发出的消息」；用 Redis Pub/Sub 把**「广播这件事」**变成所有进程都能收到的事件。核心认知：**名单不能共享**（连接对象绑在具体进程上，没法序列化），**但「该发一条消息了」这个事件可以共享**。（第 12 周共享「计数」，这一周共享「事件」。）
- **Day 6**：第十三周综合实战 —— 实现一个实时通知端点，并更新 README。

> **计划调整说明（第 13 周）**：
> **1. 把「鉴权」提前到 Day 2。** 原计划 Day 2 是「连接管理器（登记连接、管理在线用户）」、Day 4 才是鉴权。但**「管理在线用户」必须先知道用户是谁** —— 没有身份，管理器只能记一串裸连接，第 4 天再回头给它们补身份，等于把同一个模块写两遍。所以顺序改成：握手 → 鉴权 → 管理器 → 广播。
> **2. 新增 Day 5（Redis Pub/Sub）。** 原计划到「广播」就结束了，但那个广播靠的是**住在进程内存里的连接字典** —— 第 15 周用 `uvicorn --workers 4` 部署时，「用户连着 worker 1、消息从 worker 2 发出」会直接失效。这和**第 12 周 Day 3 之前的内存限流是同一个病**（状态住在进程里），解法也同源（搬到 Redis）。与其留个坑等部署那天再踩，不如在这一周收掉。**两周合起来正好是一条线：第 12 周共享「计数」，第 13 周共享「连接」。**
> **3. 本周不讲**：SSE、`socket.io` 这类封装库、心跳保活与断线重连。**心跳那件事不会消失** —— 第 15 周它会以另一个面目出现：Nginx 反向代理必须配 `Upgrade` / `Connection` 头，否则 WebSocket 连握手都过不去。
> **4. Day 5 用到了 `lifespan` 的最小形态。** Redis Pub/Sub 需要一条**常驻的订阅任务**，而「应用启动时创建、关闭时取消」这件事只有 `lifespan` 能做。它原本排在第 14 周 Day 5 —— 这里**提前使用，但不展开讲解**：今天只用到「启动时起一个后台任务、关闭时取消它」这一种写法，不碰连接池、不碰优雅关闭的细节。第 14 周那天会把它讲透。**先见过、再理解，比反过来容易。**
> **5. 今天只让「广播」跨进程，不让「私聊」跨进程。** 广播是「发给所有人」，所以每个进程收到事件后，各自发给自己那批连接就行。私聊要「发给某个具体的人」，那就得先知道**他连着哪个进程** —— 那需要按用户名路由，是另一套机制，不在今天范围内。**所以 `/msg` 在多 worker 下仍然是坏的。** 这是个已知缺口，记在这里，不假装它不存在。
> **6. 多进程这件事，单元测试测不到。** 一个 pytest 进程里**没有「第二个进程」**，所以 Day 5 的测试用一条很短的替身，把「经 Redis 转发」这一跳换成本地广播 —— **广播的行为照常测，跨进程那一段只能靠手动开两个进程验证**。这不是偷懒，是**测试边界的划定**：单元测试负责「一个进程里的逻辑」，多进程属于集成测试（第 16 周压测时会再碰一次）。

## 第五阶段：缓存、实时通信与可观测性（第 14 周）
- **Day 1**：`logging` 模块与日志分级（DEBUG / INFO / WARNING / ERROR）。
- **Day 2**：日志格式化与持久化（写入文件、按天切割）。
- **Day 3**：请求级上下文（Request ID 贯穿一次请求的所有日志）。
- **Day 4**：错误上报与堆栈记录（与第 6 周的异常处理器合流）。
- **Day 5**：`Lifespan` 启动 / 关闭钩子（连接池初始化与优雅关闭）。**第 13 周 Day 5 已经用过它的最小形态**（启动 / 取消一个后台订阅任务），这一天把它讲透：它到底管什么、为什么用 `asynccontextmanager` 写、关闭时怎么保证后台任务一定被取消、连接池该在哪建在哪关。
- **Day 6**：第十四周综合实战 —— 全项目接入统一日志，并更新 README。

## 第六阶段：部署运维与性能调优（第 15 周）
- **Day 1**：Dockerfile —— 把应用打包成镜像（基础镜像、依赖安装、启动命令）。
- **Day 2**：多阶段构建与镜像瘦身（分层缓存、`.dockerignore`）。
- **Day 3**：Docker Compose 编排（应用 + 数据库 + Redis 一键启动）。
- **Day 4**：Alembic 迁移（从第 11 周挪来）—— 让「有数据的数据库」平滑改表结构：初始化、生成迁移、升级与回滚。
- **Day 5**：Nginx 反向代理与 Gunicorn + Uvicorn 进程模型。
- **Day 6**：第十五周综合实战 —— 用 Compose 完整部署一套环境，并更新 README。

## 第六阶段：部署运维与性能调优（第 16 周）
- **Day 1**：数据库连接池配置与调优。
- **Day 2**：异步 IO 与并发瓶颈定位。
- **Day 3**：压测入门（给接口打流量、观察关键指标）。
- **Day 4**：缓存与限流在高并发下的调优。
- **Day 5**：第十六周综合实战 —— 出一份压测与调优报告，并更新 README。
- **Day 6**：**全项目通读与学习回顾**（整门课的收尾）—— 按目录逐个文件讲清"它在做什么、为什么需要它"；再按时间顺序回顾第 0 到第 16 周学过的每一项技术，说明它解决什么问题、在当前代码里的哪个位置。

> **关于 Day 6**：这不是一节"新内容"课，是**整门课的目录与索引**。学完它，你应该能对着这份代码从入口一路讲到数据库，并且知道每一层为什么存在。
> 它被放在最后而不是现在写，原因很简单：**这份代码到第 16 周还会变很多**（Redis、WebSocket、日志、Docker、Postgres），现在写的通读版到那时已经过时。

## 第七阶段：做自己的项目（第 17 周，时长自定）

> **为什么没有 Day 1 / Day 2**
> 因为"多久做完"取决于项目的规模和你自己的节奏 —— 硬凑天数只会逼出一个烂尾的项目。
> 这一阶段按**阶段**推进，每个阶段有**明确的产出物**。判断进度看产出物，不看花了几天。

**起点**：第 16 周结束，并且**自己补完** SQL 与 LLM 应用开发。
**终点**：一个部署在自己域名上、GitHub 上能点开、能对着别人讲 5 分钟的项目。

> **这一节是写给未来的自己看的。**
> 第 17 周会在**很久以后**才开始（中间要自己学 SQL、LLM 应用，还要复习 FastAPI）。所以下面把**选定的方向、难点在哪、从哪儿下手**全部写死在这里 —— 回来的时候不用重新想一遍。

### 回来之前，先确认自己具备这些

- [ ] **手写 SQL**：`SELECT` / `JOIN` / `GROUP BY` / 子查询 / 索引 / 看得懂执行计划
- [ ] **不看教程能写一个 FastAPI CRUD**（看着自己的笔记就行）
- [ ] **会调大模型接口**：普通调用 + 流式调用
- [ ] **能说清楚 embedding 是什么、向量检索大概怎么算**
- [ ] **能说清楚 RAG 的完整流程**：切块 → 嵌入 → 检索 → 拼上下文 → 生成 → 引用

**最后两条如果说不清楚，就还没准备好。** 因为它们正是这个项目的主体。

### 选题（已定）

**「结构规范助手」—— 规范条文速查 + LLM 问答。**

**一句话**：输入"梁的配筋率限值是多少"，给出条文原文和出处，还能接着追问。

**为什么是它**（不是因为它最难，是因为它同时满足三件事）：

| | |
| --- | --- |
| **做得完** | 数据是自己整理的 Markdown，不用抓取、不用清洗 |
| **有深度** | 难点不在 CRUD 上，在下面那六件事上 |
| **和你的专业咬合** | 建筑结构 + 会写后端 —— **能同时看懂"配筋率"和"向量检索"的人，面试官一天也碰不到一个** |

### 这个项目的六个真难点（也是面试会被追问的地方）

1. **检索** —— 规范里写的是"纵向受力钢筋的最小配筋百分率"，用户打的是"梁的配筋率限值"。**关键词一个都对不上。** 而纯向量检索又搞不定"第 4.2.3 条"这种精确查询。**混合检索怎么拼，是个真设计。**
2. **分块（chunking）** —— 一条规范下面常常有 1、2、3 款。**按固定字数切，会把一条完整的规定切成两半**，模型只看到半条就会答错 —— 而且答得很自信。
3. **出处** —— 答案是"0.2%"，凭什么是 0.2%？**没有出处的答案等于没有答案。** 而且模型很擅长编一个看起来很像的条号。
4. **幻觉** —— 工程领域最怕编。**"查不到"必须是一个敢说出口的答案。**（这正是第 12 周学的**负缓存**思路：**"没有"本身也是一个值得记住的答案。**）
5. **成本与限流** —— 每次提问都是真金白银。**答案缓存**（第 12 周）+ **按用户限流**（第 12 周）在这里全用上了。
6. **评测** —— 你怎么知道回答是对的？做法是**先手写 50 条"问题 → 标准答案要点"，每次改完跑一遍看命中率**。这是把第 7 周的测试思想搬到了 AI 上。

**六件事里做扎实三件，这个项目在简历上就立住了。**

### 规模策略：先做完整，再灌大

**几千条和上万条，检索方案是同一套。** 真正的断崖不在条数上，在这四件事上 —— **而它们和条数无关，和"你想做到多可靠"有关**：

- 向量要不要**持久化**（每次启动重算一万条，要多久、花多少钱）
- **新规范进来**怎么办（全量重建，还是只处理新增那几条）
- **多个人同时搜**（内存里全量比对的 CPU 扛不扛得住）
- **检索质量**怎么知道好坏

**路径**：先用 5–10 部规范（几千条）把东西**做完整**，再灌到上万条，**看它先在哪儿塌** —— 把"塌在哪儿、为什么"写进 README。

**面试时最值钱的一句话，不是"我做过一万条"，而是：**

> **"我知道这套方案到什么规模会开始吃力，以及那时该换什么。"**

### WebSocket 放哪（第 13 周学的不浪费）

**这个题目里 WebSocket 没有天然位置，不硬塞。** 但有一个位置是成立的：**流式输出** —— 模型回答要一个字一个字往外吐，那就是"服务器主动推"。

**不过业界更常用更轻的做法（SSE）。**

**建议**：先用轻的那条把功能做出来；有余力再用 WebSocket 做一遍，**把两者的对比写进 README**。

> **"我知道两条路、我选了其中一条、理由是……" —— 这比"我用了所有学过的技术"值钱一百倍。**

### 阶段与产出物

| # | 阶段 | 小单元 | 完成标志 |
| --- | --- | --- | --- |
| 1 | **定主题与范围** | 一句话 + 用户；需求清单；**明确不做什么** | 一页纸，能对人念一遍 |
| 2 | **定数据模型** | 条文结构（规范 → 章 → 节 → 条 → 款）；**切块策略**；用户 / 会话 / 收藏 | 表结构 + 每个决定的一句话理由 |
| 3 | **定接口契约** | 接口清单（方法 / 路径 / 输入 / 输出 / 状态码）；错误约定 | 一张表覆盖所有接口 |
| 4 | **搭骨架** | 复用课程项目的架构；一条接口端到端跑通 | `uvicorn` 起来，浏览器里能看到一条**真数据** |
| 5 | **逐模块实现** | ①条文入库与浏览 ②关键词检索 ③向量检索 ④混合检索 ⑤问答（拼上下文 + 调模型）⑥出处溯源 ⑦答案缓存与限流 ⑧流式输出 | **每个小单元做完，立刻测一次** |
| 6 | **评测** | 手写 50 条问答标准；跑回归看命中率 | 一个能打印命中率的脚本 |
| 7 | **整理测试** | 把阶段 5 那些"边写边测"补成完整的 `pytest` | 一跑就绿 |
| 8 | **前端** | 最小可用页面：搜索框 + 结果 + 条文原文 | 浏览器里能完整走一遍 |
| 9 | **部署上线** | NAS 或云服务器 + 域名 + HTTPS | **一个能点开的链接** |
| 10 | **写 README 并讲一遍** | 架构图 / 踩了什么坑 / 一个性能数字 | 能对着人讲 5 分钟 |

> **阶段 5 是主体，八个小单元，每个都是独立可验证的。** 卡住了就停在某一个小单元里，**不会出现"做到一半发现上面全错了"的局面**。

### 选题的评判标准（选题已定，这一节留着以后用）

**不是"哪个想法最酷"，是"哪个做得完"。**

第一次做项目，失败的原因几乎从来不是技术卡住，而是**范围失控**。

**以后想换题、或者做第二个项目时，拿这五个问题过一遍：**

| 问题 | 为什么问它 |
| --- | --- |
| **谁是用户？** | 答不上来 = 还没想清楚，不是"学得不够" |
| **他现在的麻烦是什么？** | 现在拿 Excel / 微信群 / 纸笔是怎么凑合的 |
| **数据从哪来？** | **最容易低估的一项** —— 手动录？抓取？从别的系统导？ |
| **最小可用版本是什么？** | 砍到只剩一个核心动作，它还有用吗？ |
| **讲 5 分钟时，亮点是哪一句？** | 说不出这一句，项目就还没有灵魂 |

**"数据从哪来"要单独标出来。** 它是第一次做项目最常见的隐形杀手：功能都想好了，动手才发现数据要一行行手录，或者抓下来还要清洗半天 —— **项目就卡死在第一周，而且卡住的还不是编程。**

### 这一阶段的规则（和前面 16 周完全不同）

1. **我不再给完整代码。** 我给的第一样东西是**问题**，不是答案。
2. **你卡住时，我先问"你打算怎么办"**，再决定给不给提示、给多少。
3. **代码由你写。** 我负责看 diff、挑问题、指出风险和边界情况。
4. **脚手架随进度拆除。** 前期（1-3 阶段）我引导多；中期（4-6 阶段）你为主；后期（7-9 阶段）我只做提示。
   **什么时候拆，由你说。** 我觉得"这个讲过了"不算数。
5. **推进顺序是真实项目的顺序，不是学习顺序。** 先定模型、再定接口、最后写代码；**定下来的尽量不改**。发现前面想错了，**先记下来、评估代价，再决定是改还是绕** —— 不许当场推翻重来。（前面 16 周"今天推翻昨天"是最好的学法，但它不是做项目的样子。）
6. **边写边测，不攒着测。** 每完成一小块，就在浏览器或命令行里验证一次。**先想清楚"怎么知道它对了"，再动手写。**
7. **讲解写在代码注释里，不是写在代码外面。** 每一行代码下面跟一行注释，宁可多不可少。具体规则见 `docs/教学约定.md` 第七章。

### 提前记下的三个风险

- **范围失控**：第一次做项目最常见的失败不是技术卡住，是"想做的太多 → 做不完 → 停在那里"。所以阶段 1 的重点不是"想做什么"，是**决定不做什么**。
- **永远不上场**：把"再学一点"当成推迟的理由。**这个风险在你身上是加倍的** —— 因为第 17 周明确排在"很久以后"。**给自己定一个具体的开工条件**（就是上面那份清单），或者干脆定一个日期。**别让它变成一个永远在未来的事。**
- **接不上**：中间隔了那么久，回来时手生了，当初为什么这么想也忘了。**缓解办法就是这一节本身** —— 方向、难点、下手顺序都落在纸上了。另外，**每学完一块（SQL、LLM）就回来翻一眼这一节**，让它一直活着。

